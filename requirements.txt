# dbt
# Pinned to 1.8.x — protobuf 5+ compatible.
# (dbt-core <= 1.7.x pins protobuf<5 which conflicts with modern transitive deps.)
dbt-core>=1.8.0,<1.9.0
dbt-snowflake>=1.8.0,<1.9.0
dbt-duckdb>=1.8.0,<1.9.0

# Local DuckDB engine for the quickstart
duckdb>=0.10.0,<1.4.0

# Snowflake ingestion helper
snowflake-connector-python>=3.6.0
pandas>=2.1.4

# Misc
python-dotenv>=1.0.0
PyYAML>=6.0.1
